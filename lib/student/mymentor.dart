import 'package:flutter/material.dart';

class MyMentorScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("My Mentor", textScaleFactor: 2),
      ),
    );
  }
}